# Gemini CLI Docker Environment: The Complete Engineering Manual

**Version:** 1.1.0 (Feature-Complete)
**Architecture:** Dockerized Node.js (Alpine) + Host Integration Strategy
**Compliance:** DevOps Best Practices, Zero Trust Security

---

## 📚 Оглавление

1. [Архитектурный Обзор](#1-архитектурный-обзор)
2. [Ретроспектива Разработки и Post-Mortem](#2-ретроспектива-разработки-и-post-mortem)
3. [Предварительные Требования](#3-предварительные-требования)
4. [Установка и Развертывание (Onboarding)](#4-установка-и-развертывание-onboarding)
5. [Управление Секретами и Безопасность](#5-управление-секретами-и-безопасность)
6. [Работа с Моделями (Gemini 2.5 / 3.0)](#6-работа-с-моделями)
7. [Интеграция с Git и GitHub (SSH Forwarding)](#7-интеграция-с-git-и-github)
8. [Повседневное Использование](#8-повседневное-использование)
9. [AI-Автоматизация (Git Commit)](#9-ai-автоматизация-git-commit)
10. [Внутреннее устройство (Deep Dive)](#10-внутреннее-устройство)

---

## 1. Архитектурный Обзор

### Проблема "Dependency Hell"
В экосистеме macOS установка глобальных CLI-инструментов через `npm -g` часто приводит к конфликтам версий Node.js и бинарных файлов.

### Решение: Изоляция через Контейнеризацию
Мы создали полностью изолированную среду:
1.  **Не зависит от хоста:** Все зависимости (Python, C++, Git, GitHub CLI) упакованы в Docker-образ.
2.  **Интегрирована с хостом:** Скрипт `gemini.zsh` делает использование контейнера прозрачным.
3.  **Stateful Context:** Система создает изолированную базу знаний (context) для каждого Git-проекта.

### Ключевой паттерн: "Sync In / Sync Out"
Для обхода ограничений драйвера **VirtioFS** на macOS мы используем атомарную синхронизацию:
- **BOOT:** Копирование токенов и конфигов из глобального хранилища (`~/.docker-gemini-config`) во временную папку проекта.
- **RUNTIME:** Контейнер работает с локальными копиями.
- **SHUTDOWN:** Обратное копирование обновленных токенов в глобальное хранилище.

---

## 2. Ретроспектива Разработки и Post-Mortem

### ❌ Ошибка 1: Монтирование файла поверх Volume
- **Проблема:** Docker на macOS падал с ошибкой `outside of rootfs` при попытке наложения файла на volume.
- **Решение:** Переход на архитектуру **Sync In / Sync Out**.

### ❌ Ошибка 2: Утечка конфигурации в Git
- **Проблема:** Файл `settings.json` попал в первый коммит из-за некорректного `.gitignore`.
- **Решение:** Обновление `.gitignore` и пересборка истории через `git commit --amend`.

### ❌ Ошибка 3: Отсутствие прав на Git-операции
- **Проблема:** Gemini не мог делать push.
- **Решение:** Внедрение **SSH Agent Forwarding** (проброс сокета `/run/host-services/ssh-auth.sock`).

---

## 3. Предварительные Требования

1.  **Docker Desktop for Mac:** 4.0+.
2.  **SSH Ключи:** Ed25519, добавленные в агент (`ssh-add --apple-use-keychain ~/.ssh/id_ed25519`).
3.  **Учетные записи:** Google и GitHub.

---

## 4. Установка и Развертывание (Onboarding)

### Шаг 1: Клонирование
```bash
mkdir -p ~/tools && cd ~/tools
git clone https://github.com/RussianLioN/gemini-docker-tools.git .
```

### Шаг 2: Инсталляция
```bash
./install.sh
source ~/.zshrc
```

### Шаг 3: Миграция Секретов
Вручную скопируйте `google_accounts.json` в `~/.docker-gemini-config/`, если настраиваете второй компьютер.

---

## 5. Управление Секретами и Безопасность

### Политика .gitignore
Файлы, которые **запрещено** коммитить:
```gitignore
.gemini-state/           # История чатов
google_accounts.json     # Токены Google
settings.json            # Настройки модели
gh_config/               # Токены GitHub CLI
```

---

## 6. Работа с Моделями

### Смена модели (Gemini 2.5 Pro -> 3.0 Preview)
1. Получите доступ к [Google Preview](https://goo.gle/enable-preview-features).
2. Измените файл на хосте: `~/.docker-gemini-config/settings.json`:
   ```json
   { "model": "gemini-experimental" }
   ```
3. Перезапустите `gemini`.

---

## 7. Интеграция с Git и GitHub

Используется **SSH Agent Forwarding**.

**Авторизация GitHub CLI (один раз):**
```bash
docker run -it --rm \
  --entrypoint gh \
  -v /run/host-services/ssh-auth.sock:/run/host-services/ssh-auth.sock \
  -e SSH_AUTH_SOCK=/run/host-services/ssh-auth.sock \
  -v $HOME/.docker-gemini-config/gh_config:/root/.config/gh \
  gemini-cli auth login
```

---

## 8. Повседневное Использование

### Project Mode
```bash
cd ~/my-project
gemini
```
История сохраняется в `.gemini-state` внутри проекта.

### Команды
- `@filename` — контекст файла.
- `run <cmd>` — выполнение shell-команды.

---

## 9. AI-Автоматизация (Git Commit)

Встроенная функция `aic` для генерации семантических коммитов.

### Использование
```bash
aic
```

### Алгоритм работы
1. Скрипт выполняет `git add .`.
2. Gemini анализирует `git diff`.
3. Генерирует сообщение в формате **Conventional Commits** (например, `feat: add auth logic`).
4. Запрашивает подтверждение перед коммитом.

---

## 10. Внутреннее устройство

### gemini.zsh
Основной скрипт-обертка.
- **`ensure_docker_running`**: Автозапуск Docker Desktop.
- **`gemini`**: Управляет путями, копированием конфигов и запуском контейнера.
- **`aic`**: Пайплайн для AI-коммитов.

### Dockerfile
- Base: `node:22-alpine`.
- Deps: `git`, `openssh`, `github-cli`, `python3`, `g++`.
- Entrypoint: `gemini`.

---
*Documentation generated by Gemini Docker Tooling Pipeline. 2025.*
