# Gemini CLI Docker Environment: The Definitive Engineering Manual

![Version](https://img.shields.io/badge/version-1.6.0-blue.svg?style=flat-square)
![Architecture](https://img.shields.io/badge/architecture-Docker%20%2B%20SSH%20Forwarding-orange.svg?style=flat-square)
![Security](https://img.shields.io/badge/security-Zero%20Trust-green.svg?style=flat-square)
![Status](https://img.shields.io/badge/status-production--ready-success.svg?style=flat-square)

**Enterprise-grade environment for AI-assisted development on macOS.**

---

## 📑 Оглавление (Table of Contents)

1.  [Введение и Философия Проекта](#1-введение-и-философия-проекта)
2.  [Архитектура и Технические Решения](#2-архитектура-и-технические-решения)
    *   [Изоляция через Docker (Alpine Linux)](#изоляция-через-docker-alpine-linux)
    *   [Паттерн Sync In / Sync Out](#паттерн-sync-in--sync-out-решение-проблемы-virtiofs)
    *   [Сетевая Безопасность (SSH Agent Forwarding)](#сетевая-безопасность-ssh-agent-forwarding)
    *   [Стерилизация SSH Конфигурации](#стерилизация-ssh-конфигурации-macos-compatibility)
    *   [Динамическое Монтирование Контекста](#динамическое-монтирование-контекста)
3.  [Quick Start (Шпаргалка)](#3-quick-start-шпаргалка)
4.  [Установка и Развертывание (Onboarding)](#4-установка-и-развертывание-onboarding)
5.  [Конфигурация и Авто-Обновления](#5-конфигурация-и-авто-обновления)
6.  [Управление Секретами и Безопасность](#6-управление-секретами-и-безопасность)
7.  [Рабочие Процессы (Workflows)](#7-рабочие-процессы-workflows)
    *   [Режим Чата (Gemini)](#режим-чата-gemini)
    *   [AI-Автоматизация Коммитов (AIC)](#ai-автоматизация-коммитов-aic)
    *   [Системный Режим (Gexec)](#системный-режим-gexec)
8.  [Справочник: Эталонные Конфигурации](#8-справочник-эталонные-конфигурации)
9.  [Troubleshooting и FAQ](#9-troubleshooting-и-faq)
10. [Приложение А: Настройка Host-системы с нуля](#10-приложение-а-настройка-host-системы-с-нуля)

---

## 1. Введение и Философия Проекта

В современной экосистеме разработки (особенно на macOS) управление зависимостями CLI-инструментов становится критической проблемой ("Dependency Hell"). Глобальная установка пакетов (`npm install -g`, `pip install`) приводит к:
1.  **Конфликтам версий:** Разные инструменты требуют разные версии Node.js или Python.
2.  **Загрязнению системы:** Накопление бинарных файлов и библиотек.
3.  **Невоспроизводимости:** Настройка нового ноутбука занимает часы.

### Наше Решение: Infrastructure as Code (IaC)
Мы инкапсулировали всю среду исполнения Google Gemini CLI в легковесный Docker-контейнер. Взаимодействие с хостом (macOS) осуществляется через прозрачную Bash/Zsh обертку.

**Ключевые возможности:**
*   🚀 **Zero-Config Start:** Развертывание одной командой.
*   🔒 **Secure by Design:** Секреты (Tokens, Keys) живут только в оперативной памяти контейнера.
*   🧠 **Context Aware:** Система понимает, в каком Git-проекте вы находитесь.
*   🔄 **Self-Healing & Auto-Update:** Автоматическая проверка версий и пересборка окружения.

---

## 2. Архитектура и Технические Решения

### Изоляция через Docker (Alpine Linux)
Базовый образ `node:22-alpine` модифицирован для полноценной работы. Добавлены пакеты `bash`, `coreutils` (для `ls`, `cp`), `ncurses`, `openssh`, `github-cli` и скомпилированы заглушки для `locale`.

### Паттерн Sync In / Sync Out (Решение проблемы VirtioFS)
Docker Desktop на macOS имеет ограничения при монтировании отдельных файлов внутрь volumes. Мы используем алгоритм **State Synchronization**:
1.  **Boot Phase (Sync In):** Скрипт копирует секреты из `~/.docker-gemini-config/` во временную папку проекта `.gemini-state/`.
2.  **Runtime Phase:** Docker монтирует папку `.gemini-state/` целиком.
3.  **Shutdown Phase (Sync Out):** При завершении работы скрипт копирует обновленные токены обратно в глобальное хранилище.

### Сетевая Безопасность (SSH Agent Forwarding)
Мы **не копируем** приватные ключи SSH внутрь контейнера.
Вместо этого пробрасывается Unix-сокет агента: `-v /run/host-services/ssh-auth.sock:/run/host-services/ssh-auth.sock`. Контейнер подписывает запросы ключами хоста.

### Стерилизация SSH Конфигурации (macOS Compatibility)
Это критически важный компонент совместимости.
macOS использует специфические директивы в `~/.ssh/config` (например, `UseKeychain`, `IdentityFile`), которые:
a) Не поддерживаются в Linux (вызывают ошибку синтаксиса).
b) Ссылаются на файлы, которых нет внутри контейнера.

**Решение:** Перед запуском Docker скрипт создает "очищенную" копию вашего конфига, вырезая директивы:
*   `UseKeychain`
*   `AddKeysToAgent`
*   `IdentityFile`
*   `IdentitiesOnly`

Это заставляет SSH внутри контейнера **игнорировать локальные пути к файлам** и полагаться исключительно на ключи, загруженные в SSH Agent.

---

## 3. Quick Start (Шпаргалка)

### Основные Команды

| Команда | Назначение | Сценарий использования |
| :--- | :--- | :--- |
| **`gemini`** | **Интерактивный Чат** | Генерация кода, рефакторинг. Автоматически монтирует текущую папку. |
| **`aic`** | **AI Commit** | Анализ `git diff`, генерация описания и `push`. |
| **`gexec`** | **God Mode** | Выполнение команд в контейнере. `gexec ssh server`, `gexec git push`. |

### Сценарий "Типичный цикл разработки"

```bash
# 1. Работа
cd ~/my-project
gemini
> "Проанализируй @server.js"
> /exit

# 2. Автоматическая фиксация
# Скрипт сам запустит aic.
# 🤖 Анализирую изменения...
# 📝 Предложенный коммит: feat(server): fix logic
# 🚀 Выполнить commit? [y/N]: y
# ☁️ Auto-Push... ✅
```

---

## 4. Установка и Развертывание (Onboarding)

### Требования
*   macOS (Apple Silicon / Intel).
*   Docker Desktop 4.0+.
*   SSH ключи (Ed25519).

### Шаг 1: Получение репозитория
```bash
mkdir -p ~/tools && cd ~/tools
git clone git@github.com:RussianLioN/gemini-docker-tools.git .
```

### Шаг 2: Инсталляция
```bash
./install.sh
source ~/.zshrc
```

### Шаг 3: Миграция Секретов
Если это второй компьютер, скопируйте файлы `google_accounts.json` и `settings.json` в `~/.docker-gemini-config/` вручную.

---

## 5. Конфигурация и Авто-Обновления

### Авто-Обновление (Self-Update)
Скрипт при каждом запуске проверяет версию в NPM. Если вышла новая версия Gemini CLI, Docker-образ пересобирается автоматически.

### Файлы Конфигурации
Путь: `~/.docker-gemini-config/`.
*   **`settings.json`**: Настройки модели.
    ```json
    { "model": "gemini-2.5-pro" }
    ```
    *Для Gemini 3.0 (Preview) используйте `"gemini-experimental"`.*

---

## 6. Управление Секретами и Безопасность

### Принцип Zero Trust
Секреты никогда не покидают ваш диск. Файлы исключены из Git глобально:

```gitignore
.gemini-state/           # Временное хранилище
google_accounts.json     # OAuth токены
settings.json            # Локальные настройки
gh_config/               # Токены GitHub CLI
```

---

## 7. Рабочие Процессы (Workflows)

### Режим Чата (Gemini)
Запускает контейнер с контекстом проекта.
*   Команды внутри: `ls`, `git`, `gh`.
*   Поддержка русского языка в терминале (исправлена локаль).

### AI-Автоматизация Коммитов (AIC)
Интеллектуальный агент.
1.  **Анализ:** Читает `diff` и последние 10 коммитов истории.
2.  **Генерация:** Пишет сообщение в стиле вашего проекта.
3.  **Действие:** `[Enter] Push` / `[c] Commit Only` / `[n] Cancel`.

**Security Note:** Если в проекте нет `.gitignore`, `aic` автоматически добавит туда правила защиты секретов перед коммитом.

### Системный Режим (Gexec)
Позволяет выполнять команды через настроенное окружение Docker.
Пример подключения к серверу:
```bash
gexec ssh user@myserver.com
```
*Важно: Ключ должен быть добавлен в агент на Mac (`ssh-add`).*

---

## 8. Справочник: Исходный Код и Конфигурации

Эталонные версии файлов для восстановления.

### 8.1. Dockerfile
```dockerfile
FROM node:22-alpine
ARG GEMINI_VERSION=latest
RUN apk add --no-cache bash coreutils ncurses git python3 make g++ openssh-client github-cli
ENV LANG=C.UTF-8
RUN echo "#!/bin/sh" > /usr/bin/locale && echo "echo C.UTF-8" >> /usr/bin/locale && chmod +x /usr/bin/locale
RUN echo '#!/bin/bash' > /usr/local/bin/ll && echo 'ls -lh --color=auto "$@"' >> /usr/local/bin/ll && chmod +x /usr/local/bin/ll
RUN npm install -g @google/gemini-cli@${GEMINI_VERSION} --no-audit
RUN mkdir -p /root/.ssh && chmod 700 /root/.ssh
WORKDIR /app
ENTRYPOINT ["gemini"]
```

### 8.2. gemini.zsh (SSH Sanitization Logic)
```bash
# Фрагмент логики очистки конфига
grep -vE "UseKeychain|AddKeysToAgent|IdentityFile|IdentitiesOnly" "$SSH_CONFIG_SRC" > "$SSH_CONFIG_CLEAN"
```

---

## 9. Troubleshooting и FAQ

### Q: Ошибка "no such identity" при SSH подключении
**Причина:** Контейнер игнорирует пути к файлам ключей в конфиге (они вырезаны), а в SSH-агенте ключа нет.
**Решение:** Добавьте ключ в агент на Mac.
```bash
ssh-add --apple-use-keychain ~/.ssh/id_n8n_servers
```

### Q: Ошибка "Bad configuration option: usekeychain"
**Причина:** Linux-версия SSH не понимает macOS-специфичные опции.
**Решение:** Обновите скрипт `gemini.zsh`. Новая версия автоматически создает совместимый конфиг.

### Q: Ошибка "execvp(3) failed"
**Причина:** Отсутствие `bash` в контейнере.
**Решение:** Запустите `./install.sh` для пересборки образа.

---

## 10. Приложение А: Настройка Host-системы с нуля

1.  **Установка Docker:** Скачайте Docker Desktop. Включите "Allow default Docker socket".
2.  **Генерация SSH ключей:**
    ```bash
    ssh-keygen -t ed25519 -C "email@example.com"
    eval "$(ssh-agent -s)"
    # Создаем конфиг для автозагрузки
    touch ~/.ssh/config
    echo "Host *
      AddKeysToAgent yes
      UseKeychain yes
      IdentityFile ~/.ssh/id_ed25519" >> ~/.ssh/config
    ssh-add --apple-use-keychain ~/.ssh/id_ed25519
    ```
3.  **Связь с GitHub:** Добавьте публичный ключ (`~/.ssh/id_ed25519.pub`) в настройки профиля GitHub.

---
*Documentation Version 1.6.0. Comprehensive Edition.*
