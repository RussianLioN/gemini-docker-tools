# Gemini CLI Docker Environment: The Complete Manual

**Version:** 1.0.0  
**Author:** DevOps Automation Team  
**Architecture:** Dockerized Node.js Environment with Host-Integration

---

## 📖 Оглавление

1. [Введение и Философия](#1-введение-и-философия)
2. [Архитектура Решения](#2-архитектура-решения)
3. [Предварительные Требования](#3-предварительные-требования)
4. [Установка и Развертывание](#4-установка-и-развертывание)
5. [Настройка Безопасности и Авторизация](#5-настройка-безопасности-и-авторизация)
6. [Интеграция с GitHub и SSH](#6-интеграция-с-github-и-ssh)
7. [Управление Моделями (Gemini 3.0 / 2.5 Pro)](#7-управление-моделями)
8. [Руководство по Использованию](#8-руководство-по-использованию)
9. [Troubleshooting и FAQ](#9-troubleshooting-и-faq)

---

## 1. Введение и Философия

Этот проект представляет собой **Infrastructure as Code (IaC)** решение для запуска Google Gemini CLI в полностью изолированном, воспроизводимом окружении Docker.

### Проблема
Установка Gemini CLI через глобальный `npm install -g` на хост-машине (macOS) приводит к:
- **Конфликтам зависимостей:** Конфликты с другими инструментами (например, QWEN Code, Claude Code), использующими те же бинарные имена или версии Node.js.
- **Загрязнению системы:** Накопление глобальных пакетов npm.
- **Сложности переноса:** Необходимость перенастраивать окружение на каждом новом ноутбуке.

### Решение
Мы инкапсулировали Gemini CLI, Python, Git и GitHub CLI в легковесный Docker-контейнер (Alpine Linux). Специально разработанная обвязка на Zsh () обеспечивает прозрачную интеграцию контейнера с файловой системой macOS, создавая ощущение работы с нативным приложением.

---

## 2. Архитектура Решения

Система построена на принципах **Stateless Compute, Stateful Data**.

### Компоненты
1.  **Docker Image (`gemini-cli`):**
    - База: `node:22-alpine`.
    - Инструменты: `git`, `openssh`, `github-cli`, `python3`, `make`, `g++` (для компиляции tree-sitter зависимостей).
    - Точка входа: Официальный `@google/gemini-cli`.

2.  **Zsh Wrapper (`gemini.zsh`):**
    - Автоматически проверяет и запускает Docker Daemon.
    - Определяет контекст запуска (Корень Git-проекта vs Глобальный режим).
    - Управляет "бесшовной" передачей секретов.

3.  **Механизм "Sync In / Sync Out":**
    Вместо проблемного монтирования отдельных файлов (которое вызывает ошибки VirtioFS на macOS), мы используем атомарное копирование:
    - **Start:** Скрипт копирует `google_accounts.json` (токен) и `settings.json` (конфиг) из глобального хранилища (`~/.docker-gemini-config`) во временную папку состояния проекта (`.gemini-state`).
    - **Run:** Docker монтирует папку проекта и папку состояния.
    - **Exit:** Если токены обновились внутри контейнера, скрипт копирует их обратно в глобальное хранилище.

---

## 3. Предварительные Требования

Перед началом работы убедитесь, что ваша среда соответствует требованиям:

- **Операционная Система:** macOS Sonoma (или новее).
- **Docker:** Docker Desktop for Mac (установлен и настроен).
- **Git:** Версия 2.0+.
- **SSH:** Наличие ключей Ed25519 (`~/.ssh/id_ed25519`) добавленных в `ssh-agent`.
- **Учетные записи:**
  - Google Account (для Gemini).
  - GitHub Account (для репозиториев).

---

## 4. Установка и Развертывание

Следуйте этому алгоритму для развертывания на чистом компьютере.

### Шаг 1: Клонирование Инструментария
Рекомендуется хранить этот репозиторий в отдельной папке для утилит.

```bash
mkdir -p ~/tools
cd ~/tools
git clone https://github.com/YOUR_ORG/gemini-docker-tools.git .
```

### Шаг 2: Запуск Инсталлятора
Скрипт `install.sh` выполнит всю грязную работу:
1. Проверит наличие Docker.
2. Запустит сборку образа (может занять 1-2 минуты).
3. Создаст структуру папок конфигурации в `~/.docker-gemini-config`.
4. Добавит подключение скриптов в ваш `~/.zshrc`.

```bash
./install.sh
```

После завершения **обязательно** перезагрузите конфигурацию терминала:
```bash
source ~/.zshrc
```

---

## 5. Настройка Безопасности и Авторизация

Мы придерживаемся принципа **Zero Trust** к хранению секретов в репозиториях.

### 5.1. Токены Google (Gemini Auth)
Файл `google_accounts.json` содержит OAuth-токены доступа.
- **Где хранится:** Только на хост-машине в `~/.docker-gemini-config/google_accounts.json`.
- **Как переносить:** Вручную через AirDrop/1Password при смене компьютера.
- **Первичный вход:**
  1. Запустите `gemini`.
  2. Следуйте ссылке в терминале -> Авторизация Google -> Копирование кода.
  3. После входа введите `/exit`. Скрипт автоматически сохранит созданный токен.

### 5.2. Токены GitHub (GH CLI Auth)
Настройки GitHub CLI хранятся в Volume на хосте: `~/.docker-gemini-config/gh_config`.

**Авторизация (один раз):**
```bash
# Запуск мастера авторизации изнутри контейнера
docker run -it --rm \
  --entrypoint gh \
  -v $HOME/.docker-gemini-config/gh_config:/root/.config/gh \
  gemini-cli auth login
```
Выберите: `GitHub.com` -> `SSH` -> `Login with web browser`.

---

## 6. Интеграция с GitHub и SSH

Для работы с Git внутри контейнера (команды `git push`, `gh repo create`) используется **SSH Agent Forwarding**.

### Как это работает
Мы пробрасываем сокет `/run/host-services/ssh-auth.sock` внутрь контейнера. Это позволяет Docker использовать ключи, лежащие на вашем Mac, не копируя их внутрь образа.

**Диагностика:**
Если внутри контейнера Git требует пароль, проверьте на Mac:
```bash
ssh-add -l
```
Если список пуст, добавьте ключи в Keychain:
```bash
ssh-add --apple-use-keychain ~/.ssh/id_ed25519
```

---

## 7. Управление Моделями

Конфигурация моделей управляется через файл `settings.json`.

### Текущая конфигурация (Stable)
По умолчанию установлена модель **Gemini 2.5 Pro**.
Файл: `~/.docker-gemini-config/settings.json`

```json
{
  "model": "gemini-2.5-pro",
  "security": {
    "auth": {
      "selectedType": "oauth-personal"
    }
  }
}
```

### Gemini 3.0 Pro (Preview / Experimental)
Модель 3.0 находится в раннем доступе.
1. **Waitlist:** Убедитесь, что ваш аккаунт одобрен [здесь](https://goo.gle/enable-preview-features).
2. **Активация:** Измените `"model": "gemini-2.5-pro"` на `"model": "gemini-experimental"` (или актуальный ID из документации) в файле настроек.
3. **Перезапуск:** Перезапустите `gemini`. Скрипт "Sync In" применит новую настройку.

---

## 8. Руководство по Использованию

### Основной Workflow
Перейдите в папку любого проекта и введите команду:

```bash
gemini
```

- **Project Mode:** Если вы внутри Git-репозитория, Gemini создаст папку `.gemini-state` в корне проекта. Вся история чатов будет привязана к этому проекту.
- **Global Mode:** Если вы не в проекте, история сохраняется в глобальный стейт.

### Работа с контекстом
Внутри чата используйте:
- `@filename` — добавить файл в контекст анализа.
- `/clear` — сбросить память (начать новый диалог).
- `run <command>` — выполнить shell-команду (например, `run ls -la`).

### Создание репозитория через Gemini
Благодаря интеграции `gh`, вы можете попросить Gemini:
> "Создай приватный репозиторий для этого проекта на GitHub и запушь текущий код"

Gemini выполнит `gh repo create`, `git remote add` и `git push`, используя ваши проброшенные credentials.

---

## 9. Troubleshooting и FAQ

### Q: Ошибка "Docker daemon is not running"
**A:** Скрипт `gemini.zsh` пытается запустить Docker Desktop автоматически. Если это не удается, запустите приложение Docker вручную через Spotlight.

### Q: Ошибка "outside of rootfs" или "mount failed"
**A:** Это ошибка драйвера VirtioFS на Mac при попытке сложного монтирования. Мы решили это архитектурно через копирование файлов. Убедитесь, что вы используете последнюю версию скрипта из этого репозитория (сделайте `git pull`).

### Q: Gemini показывает модель "auto" вместо "gemini-2.5-pro"
**A:** Это значит, что файл `settings.json` не попал в контейнер.
1. Проверьте наличие файла в `~/.docker-gemini-config/`.
2. Запустите `gemini` один раз — скрипт Sync In должен восстановить файл.

---
*Documentation generated automatically by Gemini Docker Tooling Assistant.*
