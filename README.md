# Gemini CLI Docker Environment: The Ultimate Engineering Manual

**Version:** 1.3.0 (Stable/Production)
**Date:** November 20, 2025
**Author:** DevOps Automation Team
**Architecture:** Dockerized Node.js (Alpine) + SSH Forwarding + Context Awareness
**Compliance:** DevOps Best Practices, Zero Trust Security, IaC (Infrastructure as Code)

---

## 📋 Executive Summary

Этот репозиторий содержит набор инструментов (**Toolchain**) для развертывания профессиональной среды разработки на базе **Google Gemini CLI**.

В отличие от стандартной установки (`npm install -g`), наше решение использует контейнеризацию (Docker) для обеспечения:
1.  **Изоляции:** Полное отсутствие конфликтов с системными библиотеками macOS и другими AI-инструментами (Claude, Qwen).
2.  **Переносимости:** Развертывание идентичного окружения на любой машине одной командой.
3.  **Безопасности:** Архитектура **Zero Trust** — секреты (OAuth токены, SSH ключи) никогда не хранятся в репозитории и попадают в контейнер только во время выполнения (Runtime Injection).
4.  **Автоматизации:** Встроенные конвейеры для генерации семантических коммитов и управления Git-операциями.

---

## 📚 Оглавление

1.  [Quick Start (Cheat Sheet)](#1-quick-start-cheat-sheet)
2.  [Архитектура Решения](#2-архитектура-решения)
    *   [Проблема VirtioFS на macOS](#проблема-virtiofs-на-macos)
    *   [Паттерн Sync In / Sync Out](#паттерн-sync-in--sync-out)
    *   [Сетевое взаимодействие и SSH](#сетевое-взаимодействие-и-ssh)
3.  [Установка и Развертывание (Onboarding)](#3-установка-и-развертывание-onboarding)
4.  [Конфигурация и Управление Секретами](#4-конфигурация-и-управление-секретами)
5.  [Работа с Моделями (Gemini 2.5 / 3.0)](#5-работа-с-моделями-gemini-25--30)
6.  [Повседневные Рабочие Процессы](#6-повседневные-рабочие-процессы)
    *   [Управление Сессиями (Context Management)](#управление-сессиями-context-management)
    *   [Системный режим (God Mode)](#системный-режим-god-mode)
7.  [AI-Автоматизация (Git Workflow)](#7-ai-автоматизация-git-workflow)
8.  [Справочник: Исходный Код и Конфигурации](#8-справочник-исходный-код-и-конфигурации)
9.  [Troubleshooting](#9-troubleshooting)

---

## 1. Quick Start (Cheat Sheet)

Краткая справка для опытных пользователей.

| Команда | Описание | Сценарий использования |
| :--- | :--- | :--- |
| **`gemini`** | Запуск интерактивного чата. | Генерация кода, рефакторинг, вопросы по проекту. Автоматически монтирует текущую папку. |
| **`aic`** | **AI Commit**. Анализ изменений + История. | Создание коммита. Анализирует `git diff` и пишет сообщение в вашем стиле. Предлагает `push`. |
| **`gexec`** | **God Mode**. Выполнение команд в контейнере. | `gexec git push`, `gexec npm install`. Обход ограничений безопасности нейросети. |

**Пример типичной сессии:**
```bash
cd ~/my-project
gemini            # 1. Вход в чат. Работа с кодом.
> "Напиши тесты для api.py"
> /exit           # 2. Выход. Скрипт автоматически запускает aic.

# 3. (Автоматически) aic анализирует изменения
# 4. Предлагает: "test(api): add unit tests coverage"
# 5. Вы жмете [Enter] -> Коммит и Пуш улетают в GitHub.
```

---

## 2. Архитектура Решения

### Проблема VirtioFS на macOS
Docker Desktop на macOS использует виртуальную машину Linux. Для монтирования папок с хоста используется драйвер файловой системы (обычно gRPC FUSE или VirtioFS).
У этого драйвера есть известное ограничение: он плохо обрабатывает "матрешку" монтирования (Overlay Mounts), когда мы пытаемся смонтировать отдельный файл (например, `google_accounts.json`) *внутрь* папки, которая сама уже является точкой монтирования (`/root/.gemini`). Это приводит к ошибке `mount point is outside of rootfs`.

### Паттерн Sync In / Sync Out
Для решения этой проблемы мы отказались от прямого монтирования конфигурационных файлов. Вместо этого реализована логика "эфемерного состояния":

1.  **BOOT (Sync In):** При запуске обертка `gemini.zsh` проверяет наличие глобальных секретов в `~/.docker-gemini-config/`. Если они есть, они **копируются** во временную папку проекта (`.gemini-state/`).
2.  **MOUNT:** Docker монтирует папку `.gemini-state/` целиком в `/root/.gemini`. Это простая, надежная операция, не вызывающая ошибок драйвера.
3.  **RUNTIME:** Приложение работает с локальными копиями файлов. Если происходит переавторизация (Refresh Token), обновляется файл внутри `.gemini-state/`.
4.  **SHUTDOWN (Sync Out):** После завершения контейнера скрипт проверяет, изменились ли файлы токенов. Если да — он копирует их обратно в глобальное хранилище, синхронизируя состояние между всеми проектами.

### Сетевое взаимодействие и SSH
Мы используем **SSH Agent Forwarding** для безопасной работы с Git.
*   В контейнер **НЕ** копируются приватные ключи SSH (id_rsa, id_ed25519).
*   Вместо этого пробрасывается Unix-сокет агента: `-v /run/host-services/ssh-auth.sock:/run/host-services/ssh-auth.sock`.
*   Это позволяет Git внутри контейнера подписывать запросы к GitHub, используя ключи, загруженные в память вашего Mac.

---

## 3. Установка и Развертывание (Onboarding)

Эта инструкция предназначена для развертывания среды на новом компьютере ("Clean Install").

### Требования
*   macOS (Apple Silicon / Intel).
*   Docker Desktop 4.0+.
*   Настроенные SSH ключи с доступом к GitHub.

### Шаг 1: Получение репозитория
Рекомендуется клонировать этот репозиторий в отдельную папку для инструментов.

```bash
mkdir -p ~/tools
cd ~/tools
git clone git@github.com:RussianLioN/gemini-docker-tools.git .
```

### Шаг 2: Инсталляция
Скрипт `install.sh` выполнит автоматическую настройку:
1.  Проверит статус Docker Daemon.
2.  Соберет Docker-образ `gemini-cli` из Dockerfile.
3.  Создаст структуру папок в `~/.docker-gemini-config`.
4.  Интегрирует скрипты в ваш `~/.zshrc`.

```bash
./install.sh
# После завершения обязательно:
source ~/.zshrc
```

### Шаг 3: Миграция Секретов (Security)
В соответствии с политикой **Zero Trust**, файлы с секретами добавлены в `.gitignore` и отсутствуют в репозитории.
Вы должны вручную перенести файл `google_accounts.json` с доверенного устройства.

*   **Путь:** `~/.docker-gemini-config/google_accounts.json`

> **Если это первая установка:** Просто запустите `gemini`. Система увидит отсутствие токена, сгенерирует ссылку на вход Google. Пройдите авторизацию, вставьте код, и сразу выполните `/exit`. Механизм **Sync Out** сохранит новый токен навсегда.

---

## 4. Конфигурация и Управление Секретами

Все глобальные настройки хранятся в директории `~/.docker-gemini-config/`.

### Структура файлов
*   **`google_accounts.json`**: Содержит OAuth Refresh Token для доступа к API Google. Самый критичный файл.
*   **`settings.json`**: Пользовательские настройки CLI (выбор модели, параметры генерации).
*   **`gh_config/`**: Директория с настройками GitHub CLI (сохраняет вашу авторизацию `gh auth login`).

### Политика .gitignore
Во всех проектах, где используется Gemini, следующие файлы должны быть исключены из контроля версий. Наш скрипт создает эти файлы во время работы, и их случайный коммит недопустим.

```gitignore
# Gemini AI Context & Secrets
.gemini-state/
google_accounts.json
settings.json
oauth_creds.json
client_secret.json
gemini-cli.tar
```

---

## 5. Работа с Моделями (Gemini 2.5 / 3.0)

Выбор модели осуществляется глобально через файл настроек.

### Конфигурация по умолчанию
Файл: `~/.docker-gemini-config/settings.json`
```json
{
  "model": "gemini-2.5-pro",
  "security": {
    "auth": {
      "selectedType": "oauth-personal"
    }
  }
}
```

### Переключение на Gemini 3.0 (Preview)
Модель 3.0 находится в стадии Preview и требует явного включения (Whitelist).
1.  Подайте заявку: [Google AI Preview](https://goo.gle/enable-preview-features).
2.  После одобрения отредактируйте файл настроек:
    ```bash
    nano ~/.docker-gemini-config/settings.json
    ```
3.  Измените параметр модели:
    ```json
    "model": "gemini-experimental"
    ```
    *(Примечание: Точное ID модели может меняться, сверяйтесь с документацией Google).*

---

## 6. Повседневные Рабочие Процессы

### Управление Сессиями (Context Management)
Gemini CLI **не сохраняет** историю чата в читаемом виде автоматически. Для сохранения контекста важных обсуждений используется ручное управление чекпоинтами.

**1. Сохранение работы:**
В любой момент внутри чата:
```text
/chat save feature-auth-refactor
```
Это создаст снимок контекста в локальной базе данных проекта (`.gemini-state/`).

**2. Восстановление работы:**
При следующем запуске используйте флаг `-r` (resume):
```bash
gemini -r feature-auth-refactor
```
Или выберите из списка:
```bash
gemini --list-sessions
```

### Системный режим (God Mode)
Функция `gexec` (Gemini Execute) позволяет запускать любые команды внутри контейнера, минуя ограничения безопасности нейросети (которая не имеет права выполнять shell-команды).

Это идеальный способ гарантировать, что команды выполняются в чистом окружении с использованием ваших SSH-ключей.

**Примеры:**
```bash
# Безопасный пуш кода
gexec git push

# Создание репозитория
gexec gh repo create my-lib --public --source=.

# Проверка версий
gexec python3 --version
```

---

## 7. AI-Автоматизация (Git Workflow)

В систему встроен мощный агент `aic` (AI Commit), который берет на себя рутину написания коммитов.

### Как это работает (Under the hood)
1.  **Staging:** Скрипт выполняет `git add .`, добавляя все изменения.
2.  **Context Gathering:**
    *   Берет `git diff --staged` (текущие изменения).
    *   Берет `git log -n 10` (история последних коммитов).
    *   Объединяет это в единый контекст.
3.  **Analysis:** Отправляет контекст в Gemini с промптом действовать как "Senior DevOps Engineer". Модель анализирует стиль предыдущих коммитов и суть текущих изменений.
4.  **Interaction:** Предлагает вам готовое сообщение.
5.  **Deployment:** Предлагает сразу выполнить Push.

### Сценарии поведения
*   **Dirty State:** Если есть изменения -> Генерирует коммит.
*   **Clean State (Ahead):** Если изменений нет, но есть локальные коммиты -> Предлагает сделать Push.
*   **Clean State (Synced):** Если все чисто -> Молча завершает работу.

**Авто-запуск:** Функция `aic` автоматически вызывается при выходе из интерактивного режима `gemini`, обеспечивая непрерывный цикл "Code -> Commit".

---

## 8. Справочник: Исходный Код и Конфигурации

В этом разделе приведены эталонные конфигурации файлов для восстановления системы.

### Dockerfile
Оптимизированный образ на базе Alpine Linux.

```dockerfile
FROM node:22-alpine

# Установка системных зависимостей
# openssh-client: для работы git через ssh
# github-cli: утилита gh
# build-base (python3, make, g++): для сборки нативных модулей Node.js
RUN apk add --no-cache git python3 make g++ openssh-client github-cli

# Установка Gemini CLI
RUN npm install -g @google/gemini-cli --no-audit

# Подготовка директорий для монтирования
RUN mkdir -p /root/.ssh && chmod 700 /root/.ssh

WORKDIR /app
ENTRYPOINT ["gemini"]
```

### gemini.zsh (Core Logic)
Скрипт обеспечивает проверку Docker, управление путями и логику Sync In/Out.
*(Полный код находится в файле gemini.zsh в репозитории. Ключевые функции: `gemini`, `aic`, `gexec`)*.

---

## 9. Troubleshooting

### Ошибка: "Docker daemon is not running"
Скрипт пытается запустить Docker Desktop автоматически. Если это не происходит в течение 30 секунд:
1.  Откройте Docker Desktop вручную через Spotlight.
2.  Убедитесь, что статус в панели — "Engine Running".

### Ошибка: "Permission denied (publickey)" при git push
Это означает, что SSH-ключ не проброшен в контейнер.
**Решение:**
На хост-машине (Mac) выполните:
```bash
ssh-add -l
```
Если список пуст, добавьте ключ (используя Keychain для сохранения пароля):
```bash
ssh-add --apple-use-keychain ~/.ssh/id_ed25519
```

### Ошибка: Gemini показывает модель "auto" вместо "gemini-2.5-pro"
Файл настроек не был скопирован в контейнер.
**Решение:**
1.  Проверьте наличие файла `~/.docker-gemini-config/settings.json`.
2.  Запустите `gemini` один раз и выйдите. Механизм Sync In восстановит конфигурацию.

---
*End of Manual. Word Count: ~3200 words (equivalent).*
