# Gemini CLI Docker Environment: The Ultimate Engineering Manual

**Version:** 1.2.0 (Stable)
**Architecture:** Dockerized Node.js + SSH Agent Forwarding + Context Awareness
**Compliance:** DevOps Best Practices, Zero Trust Security

---

## ⚡️ QUICK START (Cheat Sheet)

Самые важные команды для повседневной работы.

### 1. 🤖 Чат с AI (Контекст проекта)
Запускает Gemini CLI. Если вы в папке Git-проекта, создается изолированная база знаний.
```bash
gemini
```
*Использовать для: генерации кода, рефакторинга, вопросов по архитектуре.*

### 2. 📝 Авто-Коммит (AI Commit)
Анализирует ваши изменения (`git diff`), пишет семантическое сообщение (Conventional Commits) и делает коммит.
```bash
aic
```
*Использовать: всегда вместо `git commit -m "..."`.*

### 3. 🛠 Системный режим (God Mode)
Выполняет команды **ВНУТРИ** контейнера, используя ваши SSH-ключи и конфиги, но без ограничений нейросети.
```bash
# Пример: безопасный пуш кода, используя git внутри контейнера
gexec git push

# Пример: создание репозитория через gh CLI
gexec gh repo create my-new-tool --private --source=.
```
*Использовать для: операций с Git, GitHub CLI, запуска скриптов в чистом окружении.*

---

## 📖 Оглавление

1. [Установка и Развертывание](#1-установка-и-развертывание-onboarding)
2. [Архитектура Решения](#2-архитектура-решения)
3. [Управление Секретами](#3-управление-секретами-и-безопасность)
4. [Работа с Моделями](#4-работа-с-моделями)
5. [Интеграция с Git (SSH)](#5-интеграция-с-git-и-ssh)
6. [Внутреннее устройство](#6-внутреннее-устройство)

---

## 1. Установка и Развертывание (Onboarding)

Инструкция для настройки на новом компьютере (Computer B).

### Шаг 1: Клонирование
```bash
mkdir -p ~/tools && cd ~/tools
git clone git@github.com:RussianLioN/gemini-docker-tools.git .
```

### Шаг 2: Инсталляция
Скрипт проверит Docker, соберет образ и подключит функции в Zsh.
```bash
./install.sh
source ~/.zshrc
```

### Шаг 3: Миграция Секретов (Ручной этап)
В целях безопасности токены не хранятся в Git. Скопируйте файл с предыдущего компьютера:
- **Источник:** `~/.docker-gemini-config/google_accounts.json`
- **Назначение:** `~/.docker-gemini-config/google_accounts.json` (на новом Mac)

> **Первичный вход:** Если файла нет, просто запустите `gemini`, авторизуйтесь по ссылке и сразу напишите `/exit`. Токен сохранится автоматически.

---

## 2. Архитектура Решения

Мы решили проблему "Dependency Hell" на macOS, упаковав всё в Docker.

### Ключевой паттерн: "Sync In / Sync Out"
Для обхода проблем с файловой системой macOS (VirtioFS) мы не монтируем конфиги напрямую.
1.  **BOOT:** Скрипт копирует токены из `~/.docker-gemini-config` во временную папку проекта.
2.  **RUNTIME:** Контейнер работает локально.
3.  **SHUTDOWN:** Скрипт копирует обновленные токены обратно в глобальное хранилище.

Это гарантирует, что ваша авторизация всегда актуальна на всех проектах.

---

## 3. Управление Секретами и Безопасность

### Политика .gitignore
Файлы, которые **запрещено** коммитить (они уже добавлены в глобальный ignore проекта):
```gitignore
.gemini-state/           # История чатов
google_accounts.json     # Токены Google
settings.json            # Настройки модели
gh_config/               # Токены GitHub CLI
```

---

## 4. Работа с Моделями

Управление версией модели происходит через файл `~/.docker-gemini-config/settings.json`.

### Смена модели (Gemini 3.0 Preview)
1. Получите доступ к [Google Preview](https://goo.gle/enable-preview-features).
2. Измените файл:
   ```json
   { "model": "gemini-experimental" }
   ```
3. Перезапустите `gemini`.

---

## 5. Интеграция с Git и SSH

Система использует **SSH Agent Forwarding**. Контейнер не хранит ваши ключи, а использует сокет `/run/host-services/ssh-auth.sock`, чтобы подписывать запросы ключами с вашего Mac.

**Диагностика:**
Если `gexec git push` требует пароль:
```bash
ssh-add --apple-use-keychain ~/.ssh/id_ed25519
```

---

## 6. Внутреннее устройство

### gemini.zsh
Основной скрипт-обертка. Содержит 3 главные функции:
- **`gemini`**: Чат-бот. Управляет путями, копированием конфигов и контекстом.
- **`aic`**: Пайплайн для AI-коммитов (git add -> diff -> prompt -> commit).
- **`gexec`**: "Прозрачный" запуск команд внутри контейнера (минуя ограничения безопасности AI).

### Dockerfile
- Base: `node:22-alpine`.
- Deps: `git`, `openssh`, `github-cli`, `python3`, `g++`.

---
*Documentation generated by Gemini Docker Tooling Pipeline. 2025.*
